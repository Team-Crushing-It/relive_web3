name: storytelling_repository
description: Sharing new stories the same we've always told them
version: 1.0.0+1
publish_to: none

environment:
  sdk: ">=2.16.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  ipfs_client_flutter: ^1.0.7
  ffmpeg_kit_flutter: ^4.5.1
  path_provider: ^2.0.11
  equatable: 

dev_dependencies:
  flutter_test:
    sdk: flutter
  very_good_analysis: ^2.4.0
  build_runner: ^2.1.11
  json_serializable: ^6.2.0
